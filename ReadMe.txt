1. Go to the 'serverSide' directory and run the command 'nodemon' or 'node app.js'
2. In URL bar type 'http://localhost:3000' and press enter.
3. check the 'serverSide/uploads' directory for uploaded files.